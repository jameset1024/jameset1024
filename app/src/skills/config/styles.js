const SVGStyles = `
    .skill-wrap {
        display: flex;
        align-items: stretch;
        border-radius: 30px;
        padding: 0 15px;
        background-color: white;
        box-shadow: 0 2px 5px rgba(0,0,0, 0.35);
        margin-right: 10px;
        margin-bottom: 10px;
    }
    .svg-icon {
        padding: 5px 0;
    }
    .svg-icon svg {
        width: 50px;
        height: 50px;
        fill: var(--var-color);
    }
    .icon-name {
        display: flex;
        align-items: center;
        margin-left: 15px;
        font-size: 18px;
    }
`;

module.exports = {
    SVGStyles
}
